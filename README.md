
  # Beer Ordering App UI

  This is a code bundle for Beer Ordering App UI. The original project is available at https://www.figma.com/design/Lm09HR1nb9JQwy26vobblc/Beer-Ordering-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  